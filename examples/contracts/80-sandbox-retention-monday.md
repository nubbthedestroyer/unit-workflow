# Contract: sandbox retention, Monday board connector, return-visit CTA, exempt IPs

Module `80-sandbox-retention-monday` · risky (external service, retention of customer files) · status in `docs/plan/ledger.md`. Report schema: `{status, findings[{path,line,note}], changed[], verified[{cmd,pass}], ledgerUpdated, blockers[]}`.

## Purpose
Michael 2026-09-10: (1) Instead of "That is a lot of demos from one place today. Try again tomorrow." show a call to action: "Hey, thanks for using our tool. We still have the information from the last time you ran with this demo. Want to set a time with our team?" (2) "When a demo is run, we keep that information in a Monday board ... create one in the Platecost namespace and set up some kind of connector so this information doesn't get lost. I'd like to know the information they put in, and I'd like to retain their invoice files as well as the derived insights." (3) "Disable the demo rate limiting from my IP address because I need to test it a few times."

Fable already created the board: workspace Platecost, board **Sandbox demos** id **18430607013** (https://dtns-mgmt.monday.com/boards/18430607013). Column ids: Stage `color_mm72pgmh` (labels 0 New, 1 Processing, 2 Insights ready, 3 Kept (trial), 4 Booked, 5 Archived); Contact name `text_mm72gdf1`; Email `email_mm72dxc7`; Phone `phone_mm72g175`; Locations `text_mm72g1t0`; Revenue band `text_mm728wyd`; Started `date_mm72mqgw`; Savings low % `numeric_mm72bfr9`; Savings high % `numeric_mm72wg9h`; Savings $/mo low `numeric_mm72yxct`; Savings $/mo high `numeric_mm72sd83`; Invoices `numeric_mm72ewsv`; Line items `numeric_mm727xxw`; Menu items `numeric_mm72pn2a`; Recipes `numeric_mm7250mq`; Retained files `link_mm72ew15`; Insights `long_text_mm723k40`; Org id `text_mm72ww7p`; Pipeline lead `link_mm72t769`; Last activity `date_mm72tbbd`. The item name is the restaurant name.

## Owns
1. **Env** (lib/env.ts + .env.example + plan/deploy-checklist.md): `MONDAY_API_TOKEN` (optional; connector is a no-op with a console.warn when unset), `MONDAY_SANDBOX_BOARD_ID` (optional, default 18430607013), `SANDBOX_EXEMPT_IPS` (optional comma list; Fable already set it in Doppler dev/prd and Vercel to Michael's two addresses). Column ids live in code as a typed map in lib/monday/sandbox-board.ts, not env.
2. **Monday connector** `lib/monday/client.ts` (one function over the GraphQL API https://api.monday.com/v2, header Authorization: token, API-Version 2025-01, timeouts, one retry) and `lib/monday/sandbox-board.ts`: `upsertSandboxItem(orgId, fields)` keyed by Org id (search by the Org id column value; create_item when missing, change_multiple_column_values when present), best-effort and never blocking a user path (call inside `after()` / a job). Events: sandbox start (New + contact fields + Started + Pipeline lead link to app.platecost.io/admin/pipeline/<leadId>), first document finished (Processing), savings computed or counters changed (Insights ready + numbers + Insights long text = the savings JSON drivers and the benchmark rows in plain words), convertSandbox (Kept (trial)), Cal booking for that email (Booked; hook where 68's Cal webhook links a lead), sweep/reset (Archived + Retained files link). Add a job kind `sandbox.monday_sync` (lib/jobs/handlers/sandbox-monday.ts) so the app enqueues and the runner does the HTTP call; idempotent.
3. **Retention** (this changes the sweep's promise): before the 7-day sweep or a Start over deletes sandbox data, `archiveSandbox(orgId)` copies every file object to `s3://platecost-uploads/archive/sandbox/<orgId>/...` (same bucket, prefix carries lifecycle-free retention; add a note to scripts/aws/bucket docs) and writes `archive/sandbox/<orgId>/insights.json` (contact, counters, savings estimate, benchmark rows, plate-cost estimates, menu items, recipes) plus `manifest.json` (original keys, document kinds, vendors, dates). The Monday Retained files column gets a link to an internal admin route `app.platecost.io/admin/sandbox-archive/<orgId>` (platform-admin only, lists the files with presigned download links and the insights) — never a public S3 URL. Converted (kept) orgs are not archived; they are real orgs.
4. **Return-visit CTA** (lib/sandbox/start.ts:60-100 and app/(marketing)/try/page.tsx error rendering): when the per-IP or per-email limit trips, or the email already has a sandbox, do not show the "a lot of demos" line. Render a friendly card in journey voice: "Hey, thanks for using our tool. We still have everything from the last time you ran this demo." with two actions: "Set a time with our team" (opens the Cal modal, and marks the Monday item Booked when the booking arrives) and "Open my demo again" (sends the magic link to that email, since we know it). Update the Monday item Last activity. Keep the rate limits themselves (3 starts per IP per day, 1 sandbox per email) but `SANDBOX_EXEMPT_IPS` bypasses the IP limit in every environment (extend lib/sandbox/limits.ts allowSandboxStart; do not touch lib/auth/rate-limit.ts).
- Do NOT edit: components/try/* visuals beyond the CTA card, lib/jobs core (runner/queue), the walkthrough.
- Tests: Vitest for the connector (mocked fetch: create vs update, column value shapes, unset token = no-op), archiveSandbox (mocked S3: copies every key, writes insights.json and manifest.json, skips converted orgs), the CTA branch (limit tripped -> card, exempt IP -> allowed in production), sweep/reset call archive first; `pnpm verify` (deploy-checklist env count) green. Live check against Monday is Fable's after merge; the agent must not use a real token.

## Reference slice (copy from, do not explore further)
- lib/sandbox/start.ts:60-165; lib/sandbox/limits.ts:18-30; lib/sandbox/limits-constants.ts; lib/sandbox/sweep.ts; lib/sandbox/reset.ts (from 78); lib/sandbox/convert.ts; lib/sandbox/savings.ts (estimate shape + cache); lib/sandbox/results.ts (counters)
- lib/jobs/handlers/sandbox.ts + handlers/index.ts (registration), lib/jobs/enqueue.ts (enqueueJob, notify-once), app/api/cron/sweeps/route.ts
- lib/storage/s3.ts (client, presignGet, delete helpers; add copyObject/putObject there), lib/storage/files.ts (storage_key shape `org/<orgId>/sha256/<hash>`)
- lib/sales-pipeline/intake.ts applyCalBooking (hook for Booked), app/api/webhooks/cal/route.ts
- lib/integrations/* for the external-call-behind-one-function pattern; lib/env.ts:160-180 env style; plan/deploy-checklist.md
- app/(app)/admin/pipeline/page.tsx + lib/sales-pipeline/guard.ts requirePlatformAdmin (for the archive admin route)
- app/(marketing)/try/page.tsx (?sent / ?error rendering), components/marketing/cal-modal.tsx

## Dependencies
78 merged; migrations: none (archive lives in S3; Monday item id may be cached on organizations.sandbox_contact jsonb); env: MONDAY_API_TOKEN (Michael to supply), MONDAY_SANDBOX_BOARD_ID, SANDBOX_EXEMPT_IPS (set).

## Acceptance test
`pnpm typecheck`; `pnpm vitest run test/unit/sandbox* test/unit/monday* test/unit/try* --reporter=dot`; `pnpm verify`; Playwright: trip the email limit locally and see the return-visit card. Never kill the main dev server on 3017; run yours on another port and stop it by PID.
